# A guide that compiles

```ts
const clean = dev.vacuumClean?.();
await dev.suction?.()?.setSuctionLevel?.(2);
const scenes = clean?.scenes?.();
const first = scenes?.find((s) => s.valid);
if (first) await clean?.resumeCleaning?.();
```

<!-- typecheck: skip — a shape sketch, not a statement -->

```ts
{ notCode: true, … }
```

<!-- typecheck: host myOwnThing -->

```ts
myOwnThing(dev.info?.());
```
